#include <iostream>
#include <fstream>
#include <string>

using namespace std;

int main ()
{
	string buff;
	ofstream fout("text.txt");
	getline(cin, buff);
	fout << buff;
	fout.close();
    delete buff;
    system("pause");
		return 0;
}
